(function () {
    'use strict';

    angular.module('frontend.carexp.intro')
        .controller('IntroController', ['$rootScope', '$scope',
            function ($rootScope, $scope) {

                // $rootScope.initPage($scope);
                
                function init() {

                };

                init();
            }
        ]);

}());
