(function () {
    'use strict';

    angular.module('frontend.carexp.services')
        .controller('ServicesDetailController', ['$rootScope', '$scope',
            function ($rootScope, $scope) {

                // $rootScope.initPage($scope);
                
                function init() {

                };

                init();
            }
        ]);

}());
