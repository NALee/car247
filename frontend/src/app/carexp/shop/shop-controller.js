(function () {
    'use strict';

    angular.module('frontend.carexp.shop')
        .controller('ShopController', ['$rootScope', '$scope', '$timeout',
            function ($rootScope, $scope, $timeout) {

                // $rootScope.initPage($scope);
                
                function init() {
                    
                };

                init();
            }
        ]);

}());
