(function () {
    'use strict';

    angular.module('frontend.carexp.blog')
        .controller('BlogController', ['$rootScope', '$scope', '$timeout',
            function ($rootScope, $scope, $timeout) {

                // $rootScope.initPage($scope);
                
                function init() {
                    
                };

                init();
            }
        ]);

}());
